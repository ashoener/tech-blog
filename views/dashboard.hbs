<div class="list-group">
  {{#each posts}}
    <a
      href="/posts/{{id}}/edit"
      class="list-group-item list-group-item-action d-flex"
    >{{title}}
      <span class="ms-auto">{{renderDate createdAt}}</span></a>
  {{/each}}
</div>